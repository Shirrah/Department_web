    // Trigger the transition once the page content is fully loaded
    window.onload = function() {
        const loginForm = document.getElementById('loginForm');
        loginForm.classList.add('visible'); // Add the visible class to trigger transition
    };